<?xml version="1.0" encoding="utf-8"?>
<!--
    One resizable provider rather than three fixed ones.

    Android's convention is that a widget is resized in place, not chosen by
    size, and that also covers the launchers whose grids are not 4 columns
    wide — a set of fixed "small/medium/large" entries has to guess at a cell
    size that varies by device and by launcher. What the sizes mean is decided
    at draw time from `OPTION_APPWIDGET_MIN_WIDTH`/`_MIN_HEIGHT`, which is the
    only number that describes the space actually given.

    `targetCellWidth`/`targetCellHeight` are the API 31+ way of asking for a
    default size in grid cells and are ignored below it, so `minWidth`/
    `minHeight` still have to say the same thing in dp — hence both. 2x2 is
    the default because it is what the widget used to be, and an existing
    widget must not jump size on upgrade.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:maxResizeWidth="320dp"
    android:maxResizeHeight="320dp"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/home_widget"
    android:configure="tech.lolli.toolbox.widget.WidgetConfigureActivity"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen">
</appwidget-provider>
