[package]
name = "sbm_parser"
version = "0.1.0"
edition = "2024"
description = "ServerBox shared status parser (pure, no IO) — single source of truth for monitor and app"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
