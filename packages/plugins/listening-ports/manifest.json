{
  "id": "app.serverbox.ports",
  "version": "1.0.0",
  "abi": 2,
  "name": "Listening ports",
  "description": "What is listening on a server, and which of it is reachable from outside.",
  "license": "MIT",
  "l10n": ["en", "zh-CN"],
  "permissions": {
    "server.exec": true
  },
  "contributes": {
    "settings": { "id": "prefs", "label": "Listening ports", "icon": "port" },
    "page": {
      "id": "ports",
      "label": "Ports", "icon": "port",
      "default_on": true,
      "needs": ["shell"]
    }
  }
}
