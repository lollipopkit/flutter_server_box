function node(t,p,c){const n={t};if(p&&Object.keys(p).length>0)n.p=p;if(c&&c.length>0)n.c=c;return n}function key(n,k){return{...n,k}}function on(n,event,msg){return{...n,on:{...n.on??{},[event]:msg}}}function onTap(n,msg){return on(n,"tap",msg)}function tone(n,t){return{...n,p:{...n.p??{},tone:t}}}var column=(children,p)=>node("column",p,children);var row=(children,p)=>node("row",p,children);var expanded=(child)=>node("expanded",undefined,[child]);var padding=(all,child)=>node("padding",{all},[child]);var scroll=(children)=>node("scroll",undefined,children);var divider=()=>node("divider");var card=(children)=>node("card",undefined,children);var percent=(value,label)=>node("percent",{value,label});var tag=(label)=>node("tag",{label});var text=(value)=>node("text",{value});function shellQuote(value){return`'${value.replaceAll("'",`'\\''`)}'`}function isUsablePath(path){return path.length>0&&!path.includes(`
`)&&!path.includes("\x00")}function command(path){if(!isUsablePath(path))throw new Error(`not a path: ${path}`);const quoted=shellQuote(path);return[`printf 'df\\n'`,`df -kP ${quoted} 2>/dev/null | tail -n +2`,`printf 'du\\n'`,`du -x -d 1 -k ${quoted} 2>/dev/null`].join(`
`)}var KIB=1024;function parse(path,raw){let section=null;const children=[];let totalBytes=0;let filesystem;for(const line of raw.split(`
`)){const trimmed=line.trim();if(trimmed==="df"||trimmed==="du"){section=trimmed;continue}if(!trimmed||section===null)continue;if(section==="df"){const cols=trimmed.split(/\s+/);const size=Number(cols[1]);const used=Number(cols[2]);if(Number.isFinite(size)&&Number.isFinite(used)&&size>0){filesystem={usedBytes:used*KIB,sizeBytes:size*KIB}}continue}const at=trimmed.search(/\s/);if(at<=0)continue;const kb=Number(trimmed.slice(0,at));const found=trimmed.slice(at).trim();if(!Number.isFinite(kb)||!found)continue;if(found===path){totalBytes=kb*KIB;continue}if(!isChildOf(path,found))continue;children.push({path:found,name:basename(found),bytes:kb*KIB})}children.sort((a,b)=>b.bytes-a.bytes);return{path,totalBytes,children,filesystem}}function isChildOf(parent,child){const base=parent==="/"?"/":`${parent}/`;if(!child.startsWith(base))return false;const rest=child.slice(base.length);return rest.length>0&&!rest.includes("/")}function basename(path){const at=path.lastIndexOf("/");return at<0||path==="/"?path:path.slice(at+1)}function parentOf(path){if(path==="/"||path==="")return null;const trimmed=path.endsWith("/")?path.slice(0,-1):path;const at=trimmed.lastIndexOf("/");if(at<0)return null;return at===0?"/":trimmed.slice(0,at)}function humanBytes(bytes){const units=["B","K","M","G","T","P"];let value=bytes;let unit=0;while(value>=KIB&&unit<units.length-1){value/=KIB;unit++}const shown=unit===0||value>=10?Math.round(value):value.toFixed(1);return`${shown}${units[unit]}`}var TIMEOUT_MS=120000;var ROOT="/";var LAST_PATH_KEY="lastPath";var state={at:"idle"};var server=null;function open(_surface){return{ui:view()}}async function onHook(event){const first=event.servers[0];if(!first){state={at:"failed",path:ROOT,why:"no server"};await draw();return}server=first.server;const remembered=(await sb.store.get({scope:"server",key:LAST_PATH_KEY})).value;await scan(remembered??ROOT)}async function onEvent(msg){const m=msg;if(m.m==="open"&&m.path){await scan(m.path);return{}}if(m.m==="up"){const from=current();const parent=from===null?null:parentOf(from);if(parent!==null)await scan(parent);return{}}if(m.m==="reload"){const from=current();if(from!==null)await scan(from);return{}}return{}}function current(){switch(state.at){case"ready":return state.scan.path;case"scanning":case"failed":return state.path;case"idle":return null}}async function draw(){try{await sb.ui.patch({path:"",node:view()})}catch{}}async function scan(path){const handle=server;if(!handle)return;state={at:"scanning",path};await draw();try{const r=await sb.server.exec({server:handle,script:command(path),timeoutMs:TIMEOUT_MS});state={at:"ready",scan:parse(path,r.stdout)}}catch(e){state={at:"failed",path,why:String(e)}}await draw();if(state.at==="ready"){try{await sb.store.set({scope:"server",key:LAST_PATH_KEY,value:path})}catch{}}}function view(){if(state.at==="idle")return padding(17,text("…"));if(state.at==="scanning"){return column([bar(state.path),divider(),padding(17,text("Measuring…"))])}if(state.at==="failed"){return column([bar(state.path),divider(),padding(17,tone(text(state.why),"danger"))])}const{scan:s}=state;const fs=s.filesystem;return column([bar(s.path),padding(13,column([row([expanded(text(`${humanBytes(s.totalBytes)} in ${s.path}`)),...fs?[text(`${humanBytes(fs.usedBytes)} / ${humanBytes(fs.sizeBytes)}`)]:[]],{spacing:9}),...fs?[percent(fs.usedBytes/fs.sizeBytes,"")]:[]],{spacing:7})),divider(),expanded(scroll(s.children.length===0?[padding(17,text("Nothing under here takes any space."))]:s.children.map((c)=>rowFor(c,s.totalBytes))))])}function bar(path){const up=parentOf(path);return padding(11,row([...up===null?[]:[onTap(tag("↑"),{m:"up"})],expanded(text(path)),onTap(tag("Reload"),{m:"reload"})],{spacing:7}))}function rowFor(child,total){const share=total>0?child.bytes/total:0;return key(onTap(card([padding(11,column([row([expanded(text(child.name)),text(humanBytes(child.bytes))],{spacing:9}),percent(share,"")],{spacing:5}))]),{m:"open",path:child.path}),child.path)}var plugin_default={open,onHook,onEvent};export{open,onHook,onEvent,plugin_default as default};
