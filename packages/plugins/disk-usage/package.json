{
  "name": "@serverbox/plugin-disk-usage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/plugin.ts --outfile dist/plugin.js --format esm --minify-whitespace",
    "pack": "bun run build && bun run scripts/pack.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@serverbox/plugin-api": "file:../../plugin-api"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
