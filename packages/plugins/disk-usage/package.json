{
  "name": "@serverbox/plugin-disk-usage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/plugin.ts --outfile dist/plugin.js --format esm --minify-whitespace && bun run ../../plugin-tools/bin/stage.ts .",
    "dev": "bun run build && bun build src/plugin.ts --outfile dist/plugin.js --format esm --watch",
    "pack": "bun run build && bun run ../../plugin-tools/bin/pack.ts .",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@serverbox/plugin-api": "file:../../plugin-api"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
