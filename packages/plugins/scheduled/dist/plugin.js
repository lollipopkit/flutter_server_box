function node(t,p,c){const n={t};if(p&&Object.keys(p).length>0)n.p=p;if(c&&c.length>0)n.c=c;return n}function key(n,k){return{...n,k}}function on(n,event,msg){return{...n,on:{...n.on??{},[event]:msg}}}function onTap(n,msg){return on(n,"tap",msg)}function tone(n,t){return{...n,p:{...n.p??{},tone:t}}}var column=(children,p)=>node("column",p,children);var row=(children,p)=>node("row",p,children);var expanded=(child)=>node("expanded",undefined,[child]);var padding=(all,child)=>node("padding",{all},[child]);var scroll=(children)=>node("scroll",undefined,children);var divider=()=>node("divider");var card=(children)=>node("card",undefined,children);var tag=(label)=>node("tag",{label});var text=(value)=>node("text",{value});function shellQuote(value){return`'${value.replaceAll("'",`'\\''`)}'`}var READ_COMMAND=[`printf 'cron\\n'`,`crontab -l 2>/dev/null`,`printf 'cronsum\\n'`,`crontab -l 2>/dev/null | cksum`,`printf 'hascron\\n'`,`command -v crontab >/dev/null 2>&1 && printf 'yes\\n'`,`printf 'timers\\n'`,`systemctl list-timers --all --no-pager --no-legend 2>/dev/null`].join(`
`);function writeCommand(lines,expect){const body=lines.join(`
`);return[`cur=$(crontab -l 2>/dev/null | cksum)`,`if [ "$cur" != ${shellQuote(expect)} ]; then printf 'conflict\\n'; exit 0; fi`,`printf '%s\\n' ${shellQuote(body)} | crontab - 2>&1 || { printf 'failed\\n'; exit 0; }`,`printf 'ok\\n'`,`crontab -l 2>/dev/null | cksum`].join(`
`)}function parseWrite(raw){const lines=raw.split(`
`).map((l)=>l.trim());if(lines.includes("conflict"))return{at:"conflict"};const at=lines.indexOf("ok");if(at<0){const why=lines.filter(Boolean).join(" ")||"the write said nothing";return{at:"failed",why}}return{at:"ok",fingerprint:(lines[at+1]??"").trim()}}function parse(raw){let section=null;const cronLines=[];const sumLines=[];const timerLines=[];let hasCron=false;for(const line of raw.split(`
`)){const trimmed=line.trim();if(["cron","cronsum","hascron","timers"].includes(trimmed)){section=trimmed;continue}switch(section){case"cron":cronLines.push(line);break;case"cronsum":if(trimmed)sumLines.push(trimmed);break;case"hascron":if(trimmed==="yes")hasCron=true;break;case"timers":if(trimmed)timerLines.push(line);break}}while(cronLines.length>0&&cronLines[cronLines.length-1]===""){cronLines.pop()}return{cronLines,jobs:cronLines.map(parseCronLine).filter((j)=>j!==null),timers:timerLines.map(parseTimer).filter((t)=>t!==null),fingerprint:sumLines.join(" "),hasCron}}function parseCronLine(line,index){const trimmed=line.trim();if(!trimmed)return null;const disabled=trimmed.startsWith("#");const body=disabled?trimmed.replace(/^#+\s*/,""):trimmed;if(!body)return null;if(/^[A-Za-z_][A-Za-z0-9_]*\s*=/.test(body))return null;if(body.startsWith("@")){const at=body.search(/\s/);if(at<0)return null;return{line:index,when:body.slice(0,at),command:body.slice(at).trim(),disabled}}const fields=[];let rest=body;for(let i=0;i<5;i++){const at=rest.search(/\s/);if(at<0)return null;fields.push(rest.slice(0,at));rest=rest.slice(at).trimStart()}if(!rest)return null;if(!fields.every(isCronField))return null;return{line:index,when:fields.join(" "),command:rest,disabled}}var CRON_NAMES=new Set(["jan","feb","mar","apr","may","jun","jul","aug","sep","oct","nov","dec","sun","mon","tue","wed","thu","fri","sat"]);function isCronField(field){if(!field)return false;return field.split(",").every((part)=>{const[value,step,...rest]=part.split("/");if(rest.length>0)return false;if(step!==undefined&&!/^[0-9]+$/.test(step))return false;if(value==="*")return true;if(!value)return false;const bounds=value.split("-");return bounds.length<=2&&bounds.every(isCronTerm)})}var isCronTerm=(t)=>/^[0-9]+$/.test(t)||CRON_NAMES.has(t.toLowerCase());function parseTimer(line){const cols=line.trim().split(/\s+/);if(cols.length<3)return null;const activates=cols[cols.length-1];const unit=cols[cols.length-2];if(!unit.endsWith(".timer"))return null;const middle=cols.slice(0,cols.length-2).join(" ");const at=middle.search(/\s(left|n\/a)\s|\sleft$/);const next=at<0?middle:middle.slice(0,at+" left".length).trim();const last=at<0?"":middle.slice(at+" left".length).trim();return{unit,activates,next:next||"n/a",last:last||"n/a"}}function toggled(lines,index){const out=[...lines];const line=out[index];if(line===undefined)return out;const trimmed=line.trimStart();const indent=line.slice(0,line.length-trimmed.length);out[index]=trimmed.startsWith("#")?indent+trimmed.replace(/^#+\s?/,""):`${indent}#${trimmed}`;return out}var state={at:"loading"};var server=null;function open(_surface){return{ui:view()}}async function onHook(event){const first=event.servers[0];if(!first){state={at:"failed",why:"no server"};await draw();return}server=first.server;await read()}async function onEvent(msg){const m=msg;if(m.m==="reload"){state={at:"loading"};await draw();await read()}if(m.m==="toggle"&&typeof m.line==="number"){await toggle(m.line)}return{}}async function draw(){try{await sb.ui.patch({path:"",node:view()})}catch{}}async function read(){const handle=server;if(!handle)return;try{const r=await sb.server.exec({server:handle,script:READ_COMMAND});state={at:"ready",schedule:parse(r.stdout)}}catch(e){state={at:"failed",why:String(e)}}await draw()}async function toggle(line){const handle=server;if(!handle||state.at!=="ready")return;const{schedule}=state;const job=schedule.jobs.find((j)=>j.line===line);if(!job)return;const answer=await sb.ui.prompt({title:job.disabled?"Enable this job?":"Disable this job?",message:`${job.when}
${job.command}`,confirm:job.disabled?"Enable":"Disable"});if(answer.cancelled)return;const next=toggled(schedule.cronLines,line);try{const r=await sb.server.exec({server:handle,script:writeCommand(next,schedule.fingerprint)});const result=parseWrite(r.stdout);if(result.at==="conflict"){state={at:"ready",schedule,note:"The crontab changed on the server. Reloaded."};await draw();await read();return}if(result.at==="failed"){state={at:"ready",schedule,note:result.why};await draw();return}state={at:"ready",schedule:{...schedule,cronLines:next,jobs:parse(sectioned(next)).jobs,fingerprint:result.fingerprint}}}catch(e){state={at:"ready",schedule,note:String(e)}}await draw()}var sectioned=(lines)=>`cron
${lines.join(`
`)}
`;function view(){if(state.at==="loading")return padding(17,text("Reading…"));if(state.at==="failed"){return padding(17,tone(text(state.why),"danger"))}const{schedule:s,note}=state;return column([padding(11,row([expanded(text(`${s.jobs.length} cron · ${s.timers.length} timers`)),onTap(tag("Reload"),{m:"reload"})],{spacing:7})),...note?[padding(11,tone(text(note),"warning"))]:[],divider(),expanded(scroll([...s.hasCron?s.jobs.map(cronRow):[padding(13,tone(text("No crontab on this machine."),"muted"))],...s.timers.map(timerRow),...s.jobs.length===0&&s.timers.length===0?[padding(17,text("Nothing is scheduled."))]:[]]))])}function cronRow(job){return key(onTap(card([padding(11,column([row([expanded(job.disabled?tone(text(job.command),"muted"):text(job.command)),job.disabled?tone(tag("off"),"muted"):tag("on")],{spacing:9}),tone(text(job.when),"muted")],{spacing:3}))]),{m:"toggle",line:job.line}),`cron:${job.line}`)}function timerRow(t){return key(card([padding(11,column([row([expanded(text(t.unit)),tag("timer")],{spacing:9}),tone(text(`next ${t.next} · last ${t.last}`),"muted")],{spacing:3}))]),`timer:${t.unit}`)}var plugin_default={open,onHook,onEvent};export{open,onHook,onEvent,plugin_default as default};
